import React from "react";
import Cart from "./Cart";
import Services from "./Services";

const Main = () => {
    return (
        <main className={`main-container min-h-screen flex flex-col`}>
            <Services />
        </main>
    );
};

export default Main;